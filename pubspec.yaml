name: fruit_assassin
description: Fruit Ninja Imitator with BLE saboteur (M5Core2)

publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Flame game engine
  flame: ^1.18.0

  # BLE communication with M5Core2
  flutter_blue_plus: ^1.32.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
